#include<stdio.h>
int main(){
    int length, breadth;
    printf("Enter lenghth:");
    scanf("%d", &length);
    printf("Enter breadth:");
    scanf("%d", &breadth);
    printf("Area of rectangle is %d", length*breadth);
    return 0;

}
